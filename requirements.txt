# ─── Production requirements — container only ────────────────────────────────
# These are the ONLY packages needed to run app.py + retrieve.py + qa.py.
#
# Verified by reading imports in all three runtime files:
#   retrieve.py → faiss, numpy, openai
#   qa.py       → openai  (+ retrieve, local)
#   app.py      → streamlit  (+ qa, retrieve, local)
#
# Everything else (sentence-transformers, torch, ragas, langchain, etc.)
# is a pipeline or evaluation dependency — see requirements-dev.txt.
# Keeping this file minimal reduces image size from ~3 GB to ~400 MB and
# build time from 15+ minutes to ~2 minutes.

openai>=1.30.0
faiss-cpu>=1.8.0
numpy>=1.26.0
streamlit>=1.35.0
